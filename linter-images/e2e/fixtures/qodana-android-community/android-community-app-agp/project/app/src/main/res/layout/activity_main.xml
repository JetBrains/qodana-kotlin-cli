<?xml version="1.0" encoding="utf-8"?>
<!-- Two planted AndroidLint smells:
       1. android:text uses a hardcoded literal -> HardcodedText (guards QD-11439).
       2. <ImageView> has no android:contentDescription -> ContentDescription (second smell). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/greeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Hello, hardcoded world!" />

    <ImageView
        android:id="@+id/logo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@android:drawable/ic_menu_info_details" />

</LinearLayout>
