{
  "case": "jvm-git-state",
  "image": "qodana-jvm",
  "description": "Git-state smoke test: the basic project scanned with no .git (none) and a git-init-no-commit repo (init-no-head), the only fixture covering a non-clean git state. Each must exit 0 and produce a SARIF.",
  "run": {
    "network": "bridge",
    "passEnv": ["QODANA_LICENSE_ONLY_TOKEN"],
    "extraArgs": ["--no-statistics"],
    "variants": [
      { "id": "none", "gitState": "none" },
      { "id": "init-no-head", "gitState": "init-no-head" }
    ]
  },
  "expectExitCode": 0,
  "sarif": {
    "schemaValid": false,
    "qodanaSeverityRequired": true,
    "expectations": [
      {
        "ruleIdPattern": ".*",
        "presence": "present",
        "count": ">=1",
        "pin": "confirmed",
        "guards": [],
        "reason": "Scan must produce at least one result in both the no-.git and git-init-no-head states (id-agnostic floor)."
      }
    ]
  }
}
